1. Division: Time - O(n), Space - O(1)
   - In the past pass, iterative over the provided nums array and find the product of non-zero elements.
   - In the second pass, divide the product obtained by each number
   Note: if any of the element is negative the resultant product value in output[i] maybe negative or 0 accordingly.