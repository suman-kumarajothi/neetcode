Hash Set: Time - O(n), Space - O(n + m)
    - Iterate over the list to identify the start of a sequence(i.e. element's which doesn't contain n-1 element in list are sequence starters)
    - Once the sequence starters(s) are identified, check if s+1 element is available in the hashset and form sequence.